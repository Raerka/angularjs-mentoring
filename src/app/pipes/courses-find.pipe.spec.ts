import { CoursesFindPipe } from './courses-find.pipe';

describe('CoursesFindPipe', () => {
  it('create an instance', () => {
    const pipe = new CoursesFindPipe();
    expect(pipe).toBeTruthy();
  });
});
